spring:
  application:
    name: grpc-analytics-server

# HTTP-???? ??? Spring Boot (actuator, health)
# ?? ??????????? ? demo-rest (8080), audit-service (8081), enrichment-client (8082)
server:
  port: 8083

# ???? gRPC-??????? (????????? ?? HTTP!)
# gRPC ?????????? ???? ??????????? ?????? ?? ???? Netty,
# ??????? ??????? ?? ???? ????? ?????????? ?? Tomcat.
grpc:
  server:
    port: 9090

# Actuator ? ???????? ???????? ???????
management:
  endpoints:
    web:
      exposure:
        include: health,info

